<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Record rule to make priority field editable only by Sales Managers -->
        <!-- Note: Field-level security is handled via readonly in view XML -->
        <!-- This file can contain additional security rules if needed -->
        
        <!-- Example: You could add a record rule to auto-assign high priority orders to managers -->
        <!--
        <record id="sale_order_high_priority_manager_rule" model="ir.rule">
            <field name="name">High Priority Orders - Manager Only</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="domain_force">[('priority', '!=', 'high')]</field>
            <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
            <field name="perm_read" eval="1"/>
            <field name="perm_write" eval="1"/>
            <field name="perm_create" eval="1"/>
            <field name="perm_unlink" eval="0"/>
        </record>
        -->
    </data>
</odoo>