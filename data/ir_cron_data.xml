<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Scheduled action to log heavy orders daily -->
        <record id="ir_cron_log_heavy_orders" model="ir.cron">
            <field name="name">Log Heavy Orders Daily</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="state">code</field>
            <field name="code">model.cron_log_heavy_orders()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="doall" eval="False"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=8, minute=0, second=0)"/>
            <field name="priority">10</field>
            <field name="user_id" ref="base.user_admin"/>
        </record>
    </data>
</odoo>